const config = {
    database: {
        DATABASE: 'notebook',
        USERNAME: 'notebook',
        PASSWORD: '12345678',
        PORT: '3306',
        HOST: 'admin.dikkoo.top'
    }
}

module.exports = config;